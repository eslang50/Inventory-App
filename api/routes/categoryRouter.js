import { Router } from 'express'

const categoryRouter = Router()

// fetch all category
categoryRouter.get('/categories', )

// add an category
categoryRouter.post()

// delete an category
categoryRouter.delete()

// update one category
categoryRouter.update()
